package vocabulary

// An agent whose signature is of a function, i.e. has a result type.

type IElFunctionAgent interface {
	EvalType():BmmFunctionTypePostResult:Result=Definition.signature (  )  BMM_FUNCTION_TYPE  Post_result : Result = definition.signature
}

type ElFunctionAgent struct {
}
