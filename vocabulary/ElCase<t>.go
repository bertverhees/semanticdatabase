package vocabulary

/**
One branch of a Case table, consisting of a value constraint (the match
criterion) and a result, of the generic parameter type T.
*/

type IElCase<t> interface {
}

type ElCase<t> struct {
}
